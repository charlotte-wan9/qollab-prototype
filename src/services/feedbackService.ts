export async function sendFeedbackRequest(
  _showId: string,
  _cueId: string,
  _recipientIds: string[],
  _message: string,
): Promise<void> {
  // Prototype: email notifications are not functional
}
